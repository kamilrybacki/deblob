/target
**/*.rs.bk
.env
*.gguf

# Real experiment secrets (Task 5) — only the `.example.yaml` template with
# placeholders is checked in; see deploy/experiment/README.md.
deploy/experiment/35-experiment-secret.yaml
