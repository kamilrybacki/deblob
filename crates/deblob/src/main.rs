//! Entry point stub. Real wiring (config, Kafka/Redis adapters, the
//! management API) lands in Task 18 — see `deblob::matcher` for the
//! testable hot-path decision logic that will be wired up here.

fn main() {}
