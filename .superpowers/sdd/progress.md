# Deblob P1 — progress ledger

Branch: p1-deterministic-core
Plan: docs/superpowers/plans/2026-07-14-deblob-p1.md
Base: d07768e

Task 1: complete (commits e1fbc92..58aede3, review clean — workspace scaffold + CI)
Task 2: complete (commits 58aede3..f6f1cdf, review clean — identity types)
  MINOR (for final review): id.rs parse() uppercases body before base32 decode → accepts mixed-case IDs, weakens lowercase canonical invariant (two casings decode to same digest but != as SchemaId). Not brief-tested; tighten later.
  MINOR: no FamilyId::new_v7/parse roundtrip test (listed in Produces).
Task 3: complete (commits f6f1cdf..610583a, review clean — envelope, errors, port traits; all signatures verified)
Task 4: complete (commits 610583a..ad53292, review clean — bounded JSON parser, security boundary; depth-bomb/NaN/dup-key-via-escape all verified safe)
  MINOR (final review): over-limit field/string is fully materialized before length rejection (bounded by max_bytes overall, not a security issue; could pre-check to fail faster).
  MINOR: unpaired low-surrogate escape → Utf8Error (semantic stretch, acceptable given fixed enum).
  NOTE: oversized string VALUES reuse SizeExceeded (no dedicated variant) — adjudicated reasonable.
Task 5: complete (commits 2313009..879bef1, review Important fixed — shape extraction + canonical hashing; value/key-order independence, empty-array distinction, deterministic BTree canon, pinned golden all verified; fingerprint version tag now derived from CANONICALIZER const)
  MINOR (final review): canonical_bytes object-key escaping covers only " \ and <0x20 controls; not guaranteed to round-trip a generic JSON parser (e.g. U+2028). Fine for preimage injectivity; doc note for consumers.
Task 6: complete (commits 879bef1..4bd83d4, review Important fixed — schema monoid; associativity/commutativity/identity proptest-proven, generalized_fingerprint != raw shape fp verified)
  DEVIATION FROM BRIEF (intentional): brief said "bool flags OR'd"; corrected int_only to AND-merge (universal "all numbers integer" claim; identity().int_only=true) while keeping existential _seen flags OR. Reviewer-confirmed brief defect; int_only has zero consumers until P2.
  MINOR: added direct generalized-vs-raw fingerprint assert_ne test (was implicit).
Task 7: complete (commits 4bd83d4..d3eb2a8, two Important fixed + focused re-review clean — Redis schema vault, atomic Lua publication; CHECKPOINT CLOSED)
  FIX A: immutability compare narrowed to canonical+canonicalizer only (schema now stored as HASH) — idempotent retries with fresh provenance no longer wrongly violate.
  FIX B: Registry::publish now returns authoritative FamilyVersion (HINCRBY-allocated); caller-supplied record.version never trusted; get_schema/list_schemas return authoritative version. TRAIT SIGNATURE CHANGED in deblob-core (only impl so far).
  MINOR (final review): version cast i64->u32 truncates rather than errors (unreachable in practice).
Task 8: complete (commits d3eb2a8..17ae9f7, review clean — bucketed structural index + rebuild + verify; ShapeSummary in fingerprint; bounded per-bucket SSCAN resolve verified, golden hash independently recomputed, Task 7 invariants intact 44/44)
  MINOR (final review): rebuild_index does per-key HGET+SADD round-trips (no pipelining) — fine offline, slow on huge vault. Pre-Task-8 schema data (no `bucket` field) silently skipped by rebuild/verify (no such data on branch).
Task 9: complete (commits 17ae9f7..cde252b, review Approved + persistence-gate follow-up fixed — evidence store; TTL split verified (candidate TTL>0, audit stub permanent TTL=-1, no path sets its TTL), evidence stream XTRIM-bounded, state guard via atomic Lua (better than WATCH); persistence gate now enforced on RedisEvidence::connect too so audit stubs durable)
  MINOR (final review): audit-stub generalized_fp field redundant with key suffix; list_candidates single-SCAN-pass (standard cursor pagination, consistent with list_schemas).
Task 10: complete (commits cde252b..9c66742, review clean — runtime persistence health gate; pure evaluate_persistence parser (AND of aof_enabled/aof_last_write/rdb_bgsave/noeviction), cheap AtomicBool gate check in publish (no per-write Redis I/O), genuine freeze test, recoverable flag not latch, HealthGate Clone for /readyz; 59/59)
  MINOR (final review): no live timer-tick Degraded->Ok recovery test (logic sound, scoped out by brief). spawn_probe not yet wired into a running binary (Task 18).
Task 11: complete (commits 9c66742..5fa5fc8, review Important fixed — hot-path matcher; all 5 decision-table rows verified incl. registry-error->Unresolved (candidate-storm guard) and deterministic cand_ on miss; bucket_key HOISTED to deblob-fingerprint as single source of truth, dedup'd from matcher+index, both goldens byte-identical 8badde10; 69/69)
Task 12: complete (commits 5fa5fc8..a86bd3c, review clean — management API; constant-time bearer (subtle::ct_eq) on ALL /api/v1/*, unauth health/readyz/metrics, error envelope + status mapping, opaque cursor pagination, promote 201+Location, /readyz reads HealthGate; Promoter trait seam for Task 14; 77/77)
  MINOR (final review): malformed JSON promote body hits axum default 400 plaintext, not the error envelope (add custom rejection handler). No same-length-wrong-token test (ct_eq path correct by inspection). families list/detail/versions return 501 (no Registry trait method). Actor via spoofable X-Deblob-Actor default "api" — revisit in Task 14 audit design.
Task 13: complete (commits a86bd3c..fa4f543, review clean — cargo-fuzz harness for parser; fuzz crate isolated from workspace (exclude + own [workspace] table), verbatim target, scheduled-cron CI job w/ crash upload, 33-seed corpus; parser survived 1.19M execs 0 crashes (reviewer re-ran 376k, clean); 77/77 unaffected)
Task 14: complete (commit fa4f543..310d399 — cold lane + guarded manual promotion; ColdLane::ingest read-merge-write into EvidenceStore, per-source governor rate limit on brand-new candidates (10/min default), concrete Promoter with min_samples/min_age guards; 88/88, 11 new)
  DEVIATION FROM BRIEF (necessary, documented in-code): a single observation's own generalized_fingerprint is NOT order/variant-independent the way two MERGED profiles are (Task 6's own test only proves merge(p1,p2)==merge(p2,p1), not p1.gen_fp==p2.gen_fp) — two single-sample profiles differing by one optional field never share a fingerprint on their own. Clustering therefore uses a bounded "drop one top-level field" projection set (registered + looked-up on every ingest, both directions) rather than a single-key lookup; deliberately skips reducing to an empty-object projection (would spuriously collide unrelated single-required-field schemas) and does not generalize past one simultaneously-optional top-level field or into nested/array-element fields — scoped P1 heuristic, documented in coldlane.rs's `reduced_generalized_fps`.
  DEVIATION: EvidenceStore gained get_cluster/set_cluster (implemented in RedisEvidence, same TTL as the candidate). CoreError gained PolicyRejected, mapped to 422 in api::ApiError::from_core (distinct from Conflict's 409). Profile/FieldNode/TypeCounts now derive Serialize/Deserialize (needed to round-trip through CandidateRecord.profile); SourceCursor now derives them too (needed for DiscoveryMsg, Task 16). Added Profile::generalized_canonical_json (extracted from generalized_fingerprint's existing body-writer, no change to the pinned hash).
  MINOR (self-review): Promoter's bucket_key is derived from Profile's FieldNode tree (top-level children + a hand-rolled depth walk mirroring shape_depth), not a real Shape — Profile can't represent per-field type unions as a Shape (only Array does, via ShapeSet); documented in policy.rs's generalized_shape_summary. canonicalizer field is set to deblob-fingerprint's CANONICALIZER per the brief's explicit instruction even though the canonical string itself comes from the monoid crate's own (differently-versioned) generalized-JSON writer, not deblob-fingerprint's canonical_bytes — flagged for final review.
Task 14: complete (commits fa4f543..b09fdbd, review ❌->fixed + focused re-review Approved — cold lane + Promoter; CRITICAL ROUND-TRIP FIX)
  ORIGINAL REVIEW FOUND: promote->resolve broken by construction (promoted sch_id in deblob-monoid-v1 domain, hot-path raw_fp in deblob-canon-v1 domain — never collide; promoted schemas unresolvable, messages Provisional forever).
  FIX: candidate retains observed concrete (bucket, raw_fp) variants; promotion indexes each -> sch_id inside the atomic Lua publish; variants stored on immutable schema hash so rebuild_index restores them. sch_id unchanged. Proven end-to-end vs real Redis (single+multi-variant+rebuild). publish signature gained variant_members; EvidenceStore gained add_variant/get_variants.
  Other: CoreError::PolicyRejected->422 guard (min_samples/min_age); per-source governor rate limit; stats-only evidence (§9); clustering = "drop one top-level field" projection (P1 scope: one optional top-level field, no nested/array).
  MINOR (final review): variants field full-overwrite on idempotent republish (could drop coverage if republished after candidate variant-set expired). Self-referential bucket_member still indexed (dead weight, kept for index_it back-compat). Pre-existing canonicalizer/sch_id domain-tag note (out of scope).
Task 15: complete (commit b09fdbd..910d1ea — observability; Metrics struct wrapping a private prometheus::Registry, P1-emittable subset of the design-book canonical surface registered + non-emittable series (relay/promotions/cold-lane-lag) pre-registered so /metrics is stable across phases; all label sets bounded fixed enums, no ids/topics/messages; slm_decisions_total deliberately NOT registered (P2); HotMatcher::classify and ColdLane::ingest instrumented without changing core decision logic; real Prometheus text exposition wired into /metrics (was a placeholder); metrics::init_tracing() (JSON + RUST_LOG env-filter) for Task 18's main.rs; scripts/lint-no-payload-logs.sh CI guard wired into ci.yml; 99/99, 7 new)
  MINOR (self-review): candidate_promotions_total/relay_records_total/relay_transactions_total/cold_lane_lag_records registered but never incremented in P1 — promotion happens in policy.rs::Promoter (not one of this task's three scoped files) and relay/cold-lane-lag require Task 16/18's Kafka wiring; label-value pre-touch is skipped for these (no incrementing call site to anchor a canonical value list yet). lint-no-payload-logs.sh is line/macro-call scoped (matches the brief's own suggested grep style), not a full Rust parser — a payload variable renamed to dodge the deny-list, or split across a helper function, would not be caught; it is a best-effort CI guard, not a substitute for review.
Task 15: complete (commits b09fdbd..861e849, review clean — observability; canonical deblob_* names per design-book §6, labels bounded by const arrays + discriminant match (fate/reason/result/operation, NO id/topic labels — 2 drift-guard tests), /metrics unauth TextEncoder, classify instrumentation purely additive (decision table intact), no-payload-log lint wired into ci.yml + independently confirmed to catch ?payload; 99/99)
  MINOR (final review): brief's deblob_persistence_degraded gauge silently dropped (functionally covered by /readyz + HealthGate; not in design-book canonical set). Several metrics registered-but-not-emitted until Task 16/18 wiring (relay_records/transactions, cold_lane_lag, promotions). init_tracing exists but not called (Task 18 main stub).
Task 16: complete (commits 861e849..ef1c58e, review ✅ spec / Important = missing crash-abort-visibility test — transactional Kafka relay CHECKPOINT)
  Verified: single-txn tagged+discovery+offsets (send_offsets_to_transaction, abort on any produce/offset err), header hygiene (case-insensitive strip-all-deblob- then write-one, spoof-defended), p→p explicit partition, tombstone passthrough (detected pre-parse, null preserved), malformed->quarantine bounded reason, replay determinism byte-identical, read_committed test consumers, cooperative-sticky + pre_rebalance abort wired, FaultPoint hook (default None). Env fixes (txn replication factor=1, libcurl) TEST-HARNESS ONLY. 116/116, 7 Docker tests vs real KRaft.
  IMPORTANT (closed by Task 17): crash-abort-visibility test (fault-before-commit -> read_committed sees nothing) folded into Task 17 chaos suite.
  MINOR: tombstone early-return skips AfterProduceBeforeCommit fault check (fixed in Task 17 setup). Narrow begin_transaction/store(true) vs pre_rebalance race (Task 17 note). DiscoveryMsg.source=raw topic not per-producer (rate limiter per-topic; no producer identity without hygiene violation). Txn calls inline not spawn_blocking (Task 18 throughput).
Task 17: complete (base ef1c58e, chaos suite — crash-consistency + rebalance, CHECKPOINT)
  FIX (Task 16 review Important, closed): tombstone early-return in run_transaction_body now honors AfterProduceBeforeCommit (previously reached TransactionBody::Produced unconditionally, bypassing the chaos hook for null-payload records); fault: None behavior unchanged.
  4 Docker chaos tests vs real KRaft (crates/deblob-kafka/tests/chaos_it.rs — the relay lives in deblob-kafka, not deblob; brief's stated path was stale): abort_visibility_read_committed_sees_nothing (closes the crash-abort-visibility gap — fault after produce/before commit -> read_committed sees NOTHING on tagged+discovery, deterministic bounded wait not a race; fresh relay same group+transactional_id recovers via init_transactions fencing, exactly once, byte-identical tag), kill_between_produce_and_commit_reprocess_exactly_once (crash mid-4-record-batch -> restart recovers all 4 exactly once, headers byte-identical to an independent clean-run reference), duplicate_delivery_idempotent (same 3-record raw range replayed through 2 fully independent fresh relays -> byte-identical headers, no fault involved), rebalance_mid_stream_no_loss_no_dup (2 relay instances same group/cooperative-sticky, 20 records/4 partitions, one cancelled mid-stream -> real rebalance onto survivor, no loss/dup, both tasks return Ok; stable across 4 repeated runs). 120/120 (116+4 new), clippy clean, fmt clean.
  MINOR (self-review, not a bug): same-process simulated crash/cancel means an in-flight process_record always fully resolves its transaction before Relay::run's next cancellation check, so the graceful StreamConsumer::drop-triggered pre_rebalance callback observes transaction_open=false in practice in both the abort-visibility and rebalance tests — the wiring/mechanism is exercised and correct, but the abort_transaction() branch itself isn't forced deterministically by this black-box harness. Outcome properties (sees-nothing, exactly-once, no-loss/no-dup) fully hold regardless. Pre-existing progress.md note "begin_transaction/store(true) vs pre_rebalance race" left untouched — not in this task's scope and no test exposed it as observable.
