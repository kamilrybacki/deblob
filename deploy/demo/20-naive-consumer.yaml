# Naive consumer — parses events.tagged assuming v1; breaks on v2 drift.
apiVersion: apps/v1
kind: Deployment
metadata: {name: demo-naive, namespace: deblob-demo, labels: {app: demo-naive}}
spec:
  replicas: 1
  selector: {matchLabels: {app: demo-naive}}
  template:
    metadata: {labels: {app: demo-naive}}
    spec:
      securityContext: {runAsNonRoot: true, runAsUser: 65532, fsGroup: 65532, seccompProfile: {type: RuntimeDefault}}
      containers:
      - name: naive
        image: python:3.12-slim
        command: ["sh", "-c", "pip install --user --quiet --no-cache-dir confluent-kafka && exec python /app/naive_consumer.py"]
        env:
        - {name: HOME, value: /tmp}
        - {name: BOOTSTRAP, value: "redpanda.deblob.svc.cluster.local:9092"}
        - {name: TAGGED_TOPIC, value: "events.tagged"}
        - {name: ORIGIN_PREFIX, value: "events.demo.orders"}
        - {name: GROUP, value: "demo-naive-consumer"}
        ports: [{name: http, containerPort: 8080}]
        securityContext: {allowPrivilegeEscalation: false, readOnlyRootFilesystem: false, capabilities: {drop: [ALL]}}
        resources: {requests: {cpu: 25m, memory: 64Mi}, limits: {cpu: 250m, memory: 160Mi}}
        readinessProbe: {httpGet: {path: /healthz, port: 8080}, initialDelaySeconds: 20, periodSeconds: 5, failureThreshold: 30}
        volumeMounts:
        - {name: src, mountPath: /app, readOnly: true}
        - {name: tmp, mountPath: /tmp}
      volumes:
      - {name: src, configMap: {name: demo-naive-src}}
      - {name: tmp, emptyDir: {}}
---
apiVersion: v1
kind: Service
metadata: {name: demo-naive, namespace: deblob-demo, labels: {app: demo-naive}}
spec:
  selector: {app: demo-naive}
  ports: [{name: http, port: 8080, targetPort: 8080}]
